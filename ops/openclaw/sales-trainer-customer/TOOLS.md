# Tools

This agent must have no callable tools or loaded skills. Configure the per-agent tool profile as `minimal`, deny `session_status`, and set the per-agent skill allowlist to `[]`, leaving a text-only customer simulator.
